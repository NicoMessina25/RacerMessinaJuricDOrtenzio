package Listeners;

import Events.ResetEvent;

//------------------------------------------------>||INTERFACE||<--------------------------------------------------------\\

public interface ResetListener {
	public void listenReset(ResetEvent e);
}
