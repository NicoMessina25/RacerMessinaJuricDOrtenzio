package Listeners;

import Events.WinEvent;

//------------------------------------------------>||INTERFACE||<--------------------------------------------------------\\

public interface WinListener {
	/**
	 * 
	 * @param e
	 */
	public void listenWin(WinEvent e);
}
